---
platform: linux

image: docker:///concourse/concourse-ci

inputs:
- name: docker-terraform
  path: .
- name: terraform-release

params:
  cf_cli_version_path: terraform-release/version

run:
  path: ci/build.sh
